services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: opendoc-ai
    restart: always
    ports:
      - "3000:3000"
      - "3001:3001"
    env_file:
      - .env.production
    environment:
      - NODE_ENV=production
